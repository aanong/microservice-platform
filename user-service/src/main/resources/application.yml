server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.5.79:3306/microservice_mall?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: Scgykjyxgs@2025
  redis:
    host: 192.168.5.132
    port: 30379
    password: '$NMQ#vqc'

rocketmq:
  name-server: 192.168.5.79:9876

mall:
  cache-sync:
    topic: cache-sync-user
    consumer-group: user-cache-sync-group
    enabled: true
    max-lag-ms: 500

cache:
  sync:
    mode: invalidate
  ttl:
    detail-seconds: 300
    null-seconds: 30

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  platform:
    service-name: user-service
    slow-request-threshold-ms: 800
